<section>
    <div>
       <span> 
        <a href="/post/{{id}}">{{title}}</a>
        </span>
        <span> Posted by {{user.username}} on {{created_at}}</span>
        <br>
       <span>{{comments.length}} {{commentLabel}}</span>
    </div>
    <div>
        <p> {{post_text}}</p>
    </div>
</section>
